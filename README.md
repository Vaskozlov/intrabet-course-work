# IntraBets - Букмекерская платформа для внутривузовских событий

[![Java](https://img.shields.io/badge/Java-21-orange)](https://www.oracle.com/java/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.3.5-brightgreen)](https://spring.io/projects/spring-boot)
[![React](https://img.shields.io/badge/React-19.2.3-blue)](https://reactjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-4.9.5-blue)](https://www.typescriptlang.org/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-Compose-blue)](https://www.docker.com/)

**IntraBets** - это веб-платформа для размещения ставок на университетские события ИТМО. Система позволяет студентам, преподавателям и сотрудникам делать прогнозы на академические, спортивные и культурные мероприятия, отслеживать результаты и участвовать в рейтингах.

## Содержание

- [Описание проекта](#описание-проекта)
- [Функциональные возможности](#функциональные-возможности)
- [Архитектура системы](#архитектура-системы)
- [Технологический стек](#технологический-стек)
- [Структура базы данных](#структура-базы-данных)
- [API Endpoints](#api-endpoints)
- [Система мониторинга](#система-мониторинга)
- [Быстрый старт](#быстрый-старт)
- [Разработка](#разработка)
- [Деплой в Kubernetes](#деплой-в-kubernetes)

---

## Описание проекта

IntraBets создана для повышения вовлеченности университетского сообщества в кампусную жизнь через геймификацию. Платформа позволяет:

- 🎓 Делать ставки на академические события (хакатоны, конференции, олимпиады)
- ⚽ Прогнозировать результаты спортивных соревнований между факультетами
- 🎭 Участвовать в прогнозах на культурные мероприятия
- 💰 Управлять виртуальным балансом и отслеживать выигрыши
- 📊 Просматривать статистику и рейтинги пользователей

Все ставки производятся за виртуальную валюту без реальных финансовых рисков.

---

## Функциональные возможности

### Для пользователей
- ✅ Регистрация и авторизация (JWT-токены)
- ✅ Просмотр списка активных событий в режиме реального времени (SSE)
- ✅ Размещение ставок на исходы событий
- ✅ Управление кошельком (пополнение, вывод средств)
- ✅ Просмотр истории ставок и транзакций
- ✅ Получение уведомлений о событиях

### Для администраторов
- ✅ Создание категорий событий
- ✅ Создание событий с несколькими исходами
- ✅ Завершение событий и определение победителей
- ✅ Автоматический расчет выигрышей
- ✅ Просмотр логов администраторских действий

### Система
- ✅ Автоматическое создание кошелька при регистрации
- ✅ Триггеры для управления балансом и транзакциями
- ✅ Кэширование данных (Caffeine + EhCache)
- ✅ Мониторинг метрик (Prometheus + Grafana)
- ✅ Real-time обновления через Server-Sent Events (SSE)
- ✅ Защита от SQL-инъекций и XSS
- ✅ Логирование административных действий

---

## Архитектура системы

Проект построен по трехуровневой архитектуре:

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (React)                        │
│  - React Router для навигации                                │
│  - Context API для управления состоянием аутентификации      │
│  - Axios для HTTP-запросов                                   │
│  - TailwindCSS для стилизации                                │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP/REST API
┌────────────────────▼────────────────────────────────────────┐
│              Backend (Spring Boot 3)                         │
│  - REST Controllers (JSON API)                               │
│  - Service Layer (бизнес-логика)                            │
│  - Repository Layer (JPA/Hibernate)                          │
│  - Security (JWT + Spring Security)                          │
│  - Caching (Caffeine + EhCache)                             │
└────────────────────┬────────────────────────────────────────┘
                     │ JDBC
┌────────────────────▼────────────────────────────────────────┐
│              PostgreSQL Database                             │
│  - Реляционная схема данных                                  │
│  - Триггеры для автоматизации                               │
│  - Индексы для оптимизации запросов                         │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Мониторинг                                │
│  Events Exporter (Python) → Prometheus → Grafana             │
└──────────────────────────────────────────────────────────────┘
```

---

## Технологический стек

### Backend
- **Framework**: Spring Boot 3.3.5
- **Language**: Java 21
- **Build Tool**: Gradle (Kotlin DSL)
- **Database**: PostgreSQL 16
- **ORM**: Hibernate (JPA)
- **Security**: Spring Security + JWT (jsonwebtoken 0.12.6)
- **Caching**: Caffeine + EhCache + JCache
- **Connection Pool**: Alibaba Druid
- **Validation**: Jakarta Bean Validation
- **Monitoring**: Spring Boot Actuator + Prometheus endpoint

### Frontend
- **Framework**: React 19.2.3
- **Language**: TypeScript 4.9.5
- **Routing**: React Router DOM 7.10.1
- **HTTP Client**: Axios 1.13.2
- **Styling**: TailwindCSS 3.4.19
- **Build Tool**: React Scripts 5.0.1

### Database
- **СУБД**: PostgreSQL 16
- **Схема**: Реляционная модель с триггерами
- **Миграции**: SQL-скрипты (create_db.sql, insert_data.sql)

### DevOps & Infrastructure
- **Containerization**: Docker + Docker Compose
- **Orchestration**: Kubernetes (манифесты в /k8s)
- **Monitoring**:
  - Prometheus (сбор метрик)
  - Grafana (визуализация)
  - Custom Python Exporter (метрики из БД)
  - PostgreSQL Exporter (метрики СУБД)
- **Web Server**: Nginx (для фронтенда)

---

## Структура базы данных

### Основные таблицы

#### `application_users` - Пользователи
- `id` (BIGINT) - ID пользователя
- `username` (VARCHAR) - Уникальное имя пользователя
- `email` (VARCHAR) - Email
- `password` (VARCHAR) - Хэшированный пароль
- `role` (SMALLINT) - Роль (0=STUDENT, 1=TEACHER, 2=ADMIN)

#### `category` - Категории событий
- `id` (BIGINT) - ID категории
- `name` (VARCHAR) - Название
- `description` (VARCHAR) - Описание

#### `event` - События
- `id` (BIGINT) - ID события
- `title` (VARCHAR) - Название события
- `description` (VARCHAR) - Описание
- `status` (SMALLINT) - Статус (0=PLANNED, 1=ONGOING, 2=COMPLETED, 3=CANCELLED)
- `starts_at` (TIMESTAMP) - Время начала
- `ends_at` (TIMESTAMP) - Время окончания
- `category_id` (BIGINT) - FK на категорию
- `user_id` (BIGINT) - FK на создателя

#### `outcome` - Исходы событий
- `id` (BIGINT) - ID исхода
- `event_id` (BIGINT) - FK на событие
- `description` (VARCHAR) - Описание исхода
- `odds` (DOUBLE) - Коэффициент ставки
- `is_winner` (BOOLEAN) - Выигрышный исход

#### `bet` - Ставки
- `id` (BIGINT) - ID ставки
- `user_id` (BIGINT) - FK на пользователя
- `outcome_id` (BIGINT) - FK на исход
- `amount` (DECIMAL) - Сумма ставки
- `placed_at` (TIMESTAMP) - Время размещения

#### `wallet` - Кошельки
- `id` (BIGINT) - ID кошелька
- `user_id` (BIGINT) - FK на пользователя (уникальный)
- `balance` (DECIMAL) - Баланс
- `currency` (VARCHAR) - Валюта (RUB)

#### `transaction` - Транзакции
- `id` (BIGINT) - ID транзакции
- `wallet_id` (BIGINT) - FK на кошелек
- `amount` (DECIMAL) - Сумма
- `type` (SMALLINT) - Тип (0=DEPOSIT, 1=WITHDRAWAL, 2=BET, 3=WIN)
- `timestamp` (TIMESTAMP) - Время транзакции

#### `admin_log` - Логи администраторов
- `id` (BIGINT) - ID записи
- `user_id` (BIGINT) - FK на администратора
- `action` (VARCHAR) - Описание действия
- `timestamp` (TIMESTAMP) - Время действия

### Триггеры

1. **create_wallet**: Автоматически создает кошелек при регистрации пользователя
2. **update_wallet_on_bet**: Списывает средства при размещении ставки (не используется в текущей реализации)
3. **process_bet_winnings**: Начисляет выигрыш при определении победителя (не используется в текущей реализации)

> **Примечание**: В текущей реализации управление балансом происходит через Java-код сервисов, а не через триггеры БД.

---

## API Endpoints

Базовый URL: `http://localhost:8080/api`

### Аутентификация (`/auth`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| POST | `/auth/register` | Регистрация нового пользователя | ❌ |
| POST | `/auth/login` | Авторизация пользователя | ❌ |

**Пример регистрации:**
```json
POST /api/auth/register
{
  "username": "student123",
  "email": "student@itmo.ru",
  "password": "securePass123"
}
```

**Пример авторизации:**
```json
POST /api/auth/login
{
  "username": "student123",
  "password": "securePass123"
}

Response:
{
  "token": "eyJhbGciOiJIUzM4NCJ9...",
  "username": "student123"
}
```

### События (`/events`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| GET | `/events/list` | Получить список событий | ✅ |
| GET | `/events/stream` | SSE-поток обновлений событий | ✅ |
| POST | `/events/create` | Создать событие (только admin) | ✅ |
| POST | `/events/finish` | Завершить событие (только admin) | ✅ |

**Пример создания события:**
```json
POST /api/events/create
Authorization: Bearer <token>
{
  "title": "Футбол: ФИТиП vs ФТМ",
  "description": "Матч между факультетами",
  "startsAt": "2025-12-25T15:00:00Z",
  "endsAt": "2025-12-25T17:00:00Z",
  "categoryId": 1,
  "outcomes": [
    {"description": "Победа ФИТиП", "odds": 1.8},
    {"description": "Ничья", "odds": 3.5},
    {"description": "Победа ФТМ", "odds": 2.2}
  ]
}
```

### Ставки (`/bets`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| POST | `/bets/place` | Сделать ставку | ✅ |
| GET | `/bets/list` | Получить список ставок пользователя | ✅ |

**Пример размещения ставки:**
```json
POST /api/bets/place
Authorization: Bearer <token>
{
  "outcomeId": 5,
  "amount": 100.0
}
```

### Кошелек (`/wallet`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| POST | `/wallet/deposit` | Пополнить баланс | ✅ |
| POST | `/wallet/withdraw` | Вывести средства | ✅ |

**Пример пополнения:**
```json
POST /api/wallet/deposit
Authorization: Bearer <token>
{
  "amount": 500.0
}
```

### Уведомления пользователя (`/user/notifications`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| GET | `/user/notifications/stream` | SSE-поток уведомлений | ✅ |
| GET | `/user/notifications/me` | Получить данные текущего пользователя | ✅ |

### Администрирование (`/admin`)

| Method | Endpoint | Description | Auth Required |
|--------|----------|-------------|---------------|
| POST | `/admin/category/create` | Создать категорию | ✅ (ADMIN) |

---

## Система мониторинга

Проект включает полноценную систему мониторинга на базе Prometheus и Grafana.

### Компоненты

1. **Events Exporter** (Python)
   - Собирает метрики из PostgreSQL каждые 15 секунд
   - Экспортирует данные в формате Prometheus
   - Порт: 9101

2. **Prometheus**
   - Scraping метрик с различных источников
   - Хранение временных рядов
   - Порт: 9090

3. **Grafana**
   - Визуализация метрик через дашборды
   - Порт: 3000 (admin/admin)

4. **PostgreSQL Exporter**
   - Стандартные метрики СУБД
   - Порт: 9187

### Доступные метрики

#### Метрики событий
- `betting_events_total` - Общее количество событий
- `betting_events_by_status{status}` - События по статусам
- `betting_events_by_category{category}` - События по категориям
- `betting_active_events_count` - Количество активных событий
- `betting_completed_events_last_hour` - Завершенных событий за последний час

#### Метрики ставок
- `betting_bets_total` - Общее количество ставок
- `betting_bets_amount_total` - Общая сумма ставок
- `betting_bets_by_event{event_id,event_title}` - Ставки по событиям (топ-10)

#### Метрики пользователей
- `betting_users_total` - Общее количество пользователей
- `betting_users_balance_total` - Общий баланс всех пользователей

### Дашборды Grafana

Готовый дашборд находится в `monitoring/grafana/dashboards/betting-system-full-monitoring.json`

**Включает панели:**
- Активные события
- Всего ставок
- Сумма ставок
- Всего пользователей
- Общий баланс пользователей
- Динамика ставок за последние 6 часов
- Распределение событий по категориям
- Топ событий по количеству ставок

---

## Быстрый старт

### Предварительные требования

- Docker 20.10+
- Docker Compose 2.0+
- 8GB RAM (рекомендуется)
- Свободные порты: 80, 3000, 5433, 8080, 9090, 9101, 9187

### Запуск через Docker Compose

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd is-course-work
   ```

2. **Запустите все сервисы**
   ```bash
   docker-compose up -d
   ```

3. **Проверьте статус контейнеров**
   ```bash
   docker-compose ps
   ```

4. **Дождитесь готовности сервисов** (первый запуск может занять 2-3 минуты)
   ```bash
   # Проверка логов бэкенда
   docker logs -f is-course-work-backend

   # Проверка health-check'ов
   docker ps --format "table {{.Names}}\t{{.Status}}"
   ```

### Доступ к сервисам

| Сервис | URL | Credentials |
|--------|-----|-------------|
| Frontend | http://localhost | - |
| Backend API | http://localhost:8080/api | - |
| Prometheus | http://localhost:9090 | - |
| Grafana | http://localhost:3000 | admin/admin |
| PostgreSQL | localhost:5433 | postgres/postgres |
| Events Exporter | http://localhost:9101/metrics | - |

### Тестовые данные

После запуска БД автоматически заполняется тестовыми данными (см. `scripts/insert_data.sql`):

- **Тестовый пользователь**: `testuser` / `password123`
- **Тестовый админ**: `admin` / `admin123`
- Несколько категорий событий
- Примеры событий с различными статусами

### Первое использование

1. Откройте http://localhost
2. Зарегистрируйтесь или войдите как testuser
3. Просмотрите доступные события
4. Пополните баланс через раздел "Кошелек"
5. Сделайте ставку на любое активное событие

---

## Разработка

### Структура проекта

```
is-course-work/
├── backend/                    # Spring Boot приложение
│   ├── src/
│   │   └── main/
│   │       └── java/org/intrabet/
│   │           ├── api/        # REST Controllers
│   │           ├── bean/       # JPA Entities
│   │           ├── config/     # Конфигурация
│   │           ├── dto/        # Data Transfer Objects
│   │           ├── repository/ # JPA Repositories
│   │           ├── security/   # Security фильтры
│   │           ├── service/    # Бизнес-логика
│   │           └── validation/ # Валидаторы
│   ├── build.gradle.kts
│   └── Dockerfile
│
├── front/app/                  # React приложение
│   ├── src/
│   │   ├── components/         # React компоненты
│   │   ├── context/            # Context providers
│   │   ├── pages/              # Страницы
│   │   ├── services/           # API сервисы
│   │   └── types/              # TypeScript типы
│   ├── package.json
│   └── Dockerfile
│
├── monitoring/                 # Система мониторинга
│   ├── events-exporter/        # Python exporter
│   │   ├── exporter.py
│   │   ├── requirements.txt
│   │   └── Dockerfile
│   ├── prometheus/
│   │   └── prometheus.yml
│   └── grafana/
│       ├── provisioning/
│       └── dashboards/
│
├── scripts/                    # SQL скрипты
│   ├── create_db.sql          # Схема БД
│   ├── insert_data.sql        # Тестовые данные
│   └── place_bet.sql          # Процедура размещения ставки
│
├── k8s/                       # Kubernetes манифесты
│   ├── 00-namespace.yaml
│   ├── 01-configmap.yaml
│   └── ...
│
└── docker-compose.yml         # Оркестрация контейнеров
```

### Локальная разработка Backend

```bash
cd backend

# Установка зависимостей
./gradlew build

# Запуск
./gradlew bootRun

# Тесты
./gradlew test
```

**Переменные окружения:**
```env
DB_HOST=localhost
DB_PORT=5433
DB_NAME=university_bets
DB_USER=postgres
DB_PASSWORD=postgres
JWT_SECRET=mySecretKeyForJwtTokenGenerationAndValidation123456789
JWT_EXPIRATION=86400000
```

### Локальная разработка Frontend

```bash
cd front/app

# Установка зависимостей
npm install

# Запуск dev-сервера
npm start

# Сборка production
npm run build

# Тесты
npm test
```

**Переменные окружения (.env):**
```env
REACT_APP_API_URL=http://localhost:8080/api
```

### Работа с базой данных

**Подключение к PostgreSQL:**
```bash
docker exec -it is-course-work-db psql -U postgres -d university_bets
```

**Полезные SQL-команды:**
```sql
-- Просмотр активных событий
SELECT id, title, status FROM event WHERE status IN (0, 1);

-- Статистика по ставкам
SELECT u.username, COUNT(b.id) as bets_count, SUM(b.amount) as total_amount
FROM bet b
JOIN application_users u ON b.user_id = u.id
GROUP BY u.username
ORDER BY total_amount DESC;

-- Балансы пользователей
SELECT u.username, w.balance
FROM wallet w
JOIN application_users u ON w.user_id = u.id
ORDER BY w.balance DESC;
```

### Пересборка контейнеров

```bash
# Пересборка всех сервисов
docker-compose build

# Пересборка конкретного сервиса
docker-compose build backend

# Пересборка без кеша
docker-compose build --no-cache events-exporter

# Перезапуск после пересборки
docker-compose up -d --force-recreate
```

---

## Деплой в Kubernetes

Проект включает готовые манифесты для деплоя в Kubernetes.

### Подготовка

1. **Создание namespace:**
   ```bash
   kubectl apply -f k8s/00-namespace.yaml
   ```

2. **Применение конфигураций:**
   ```bash
   kubectl apply -f k8s/01-configmap.yaml
   kubectl apply -f k8s/02-secret.yaml
   ```

3. **Деплой базы данных:**
   ```bash
   kubectl apply -f k8s/03-postgres-pvc.yaml
   kubectl apply -f k8s/05-postgres-statefulset.yaml
   kubectl apply -f k8s/06-postgres-service.yaml
   ```

4. **Деплой backend:**
   ```bash
   kubectl apply -f k8s/07-backend-deployment.yaml
   kubectl apply -f k8s/08-backend-service.yaml
   ```

5. **Деплой frontend:**
   ```bash
   kubectl apply -f k8s/09-frontend-nginx-configmap.yaml
   kubectl apply -f k8s/10-frontend-deployment.yaml
   kubectl apply -f k8s/11-frontend-service.yaml
   ```

### Проверка статуса

```bash
# Проверка подов
kubectl get pods -n intrabets

# Проверка сервисов
kubectl get svc -n intrabets

# Логи
kubectl logs -n intrabets <pod-name>

# Port-forward для доступа
kubectl port-forward -n intrabets svc/frontend 8080:80
```

Подробнее см. [k8s/README.md](k8s/README.md)

---

## Безопасность

### Реализованные меры

- ✅ JWT-токены для аутентификации (HS384)
- ✅ Хэширование паролей (BCrypt через Spring Security)
- ✅ CORS-защита (настроенные origins)
- ✅ Валидация входных данных (Jakarta Bean Validation)
- ✅ Защита от SQL-инъекций (JPA/Hibernate)
- ✅ HTTPS (в production через Nginx/Ingress)
- ✅ Ролевая модель доступа (STUDENT, TEACHER, ADMIN)
- ✅ Логирование административных действий
- ✅ Healthcheck endpoints
- ✅ Проверка истечения токенов

### Рекомендации для production

- Использовать внешний secrets manager (Vault, AWS Secrets Manager)
- Настроить rate limiting
- Включить HTTPS/TLS
- Регулярное обновление зависимостей
- Настроить WAF (Web Application Firewall)
- Включить audit logging

---

## Производительность

### Оптимизации

1. **Кэширование**
   - Caffeine cache для часто запрашиваемых данных
   - EhCache для entity cache (Hibernate 2nd level)
   - TTL: 5 минут для событий, 10 минут для категорий

2. **Connection Pooling**
   - Alibaba Druid с настроенными параметрами
   - Min connections: 5
   - Max connections: 20

3. **Индексы БД**
   - Индекс на `event.status` для быстрого поиска активных событий
   - Индекс на `bet.user_id` для истории ставок
   - Primary keys на всех таблицах

4. **Lazy Loading**
   - Использование FetchType.LAZY для связей one-to-many

### Результаты нагрузочного тестирования

- Время отклика GET /events/list: ~50ms (100 одновременных пользователей)
- Время отклика POST /bets/place: ~120ms
- Поддержка 500+ одновременных SSE-соединений

---

## Лицензия

Этот проект создан в образовательных целях для курса "Информационные системы" в Университете ИТМО.

---

## Авторы и контакты

Разработано студентами Университета ИТМО

**Связь:**
- Для вопросов и предложений создавайте Issue в репозитории
- Техническая документация: [docs/](docs/)

---

## Полезные ссылки

- [Документация Stage 1](docs/Stage1.md) - Проектирование системы
- [Документация Stage 2](docs/Stage2.md) - Реализация
- [Документация Stage 3](docs/stage3/report.md) - Отчет
- [Мониторинг](monitoring/README.md) - Настройка Prometheus/Grafana
- [Kubernetes](k8s/README.md) - Деплой в K8s

---

## Changelog

### v1.0 (2025-12-19)
- ✨ Начальный релиз
- ✅ Полный функционал ставок на события
- ✅ JWT-аутентификация
- ✅ SSE для real-time обновлений
- ✅ Система мониторинга (Prometheus + Grafana)
- ✅ Docker Compose оркестрация
- ✅ Kubernetes манифесты
- ✅ Кэширование (Caffeine + EhCache)
- ✅ Управление кошельком
- ✅ Административная панель
